image: node:latest

pages:
  # Only need deploy stage
  stage: deploy
  cache:
    paths:
      - node_modules/
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - public
  publish: public
  only:
    - main